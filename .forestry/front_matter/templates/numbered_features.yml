---
label: numbered_features
hide_body: true
fields:
- name: numbered_features
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: true
    label: title
  - name: text
    type: text
    config:
      required: true
    label: text
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: image_link
    type: text
    config:
      required: true
    label: image_link
  config:
    min: 
    max: 
    labelField: 
  label: numbered_features
